[profile.default]
src = "src"
out = "out"
libs = ["lib"]
solc_version = "0.8.26"
optimizer = true
optimizer_runs = 200

[fuzz]
runs = 12  # fork fuzz hits the live RPC; keep it modest

# See https://book.getfoundry.sh/reference/config for more options.
